<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Enrolled Students</title>
  </head>
  <body>
    <h1><%= dStudent.Subject.subject_name %></h1>
    <h3><a href="/subject">return to subject list</a></h3>
    <table style="width:80%" border="1">
      <tr>
        <th> Name </th>
        <th> Score </th>
      </tr>
      <tr>
        <td> <%= dStudent.Student.first_name + ' ' + dStudent.Student.last_name %> </td>
        <% if(dStudent.score) { %>
          <td><%= dStudent.score %></td>
        <% } else { %>
        <td><a href="/subject/<%= dStudent.Subject.id %>/<%= dStudent.Student.id %>/giveScore"> Give Score </a></td>
        <% } %>
      </tr>
    </table>
  </body>
</html>
