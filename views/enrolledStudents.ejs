<!DOCTYPE html>
<html>
<head>
    <title>Page Title</title>
</head>
<body>
    <style>
        body {
            text-align: center;
    
        }
    
        table {
            margin: auto;
        }
    </style>
    <h1><%=  subject.name   %></h1>
    <p><a href="/subjects">return to subject list</a></p>

    <table border=1>
        <tr>
            <th>Name</th>
            <th>Score</th>
        </tr>
        <%  subject.Students.forEach(student=>{ %>
        <tr>
            <% let fullName = student.first_name +' '+ student.last_name %>
            <td>  <%= fullName %>  </td>
            <td><%if(student.SubjectStudents.score == null){ %>
                <a href="students/<%=student.id%>/givescore">Give Score</a>
            <% }else{ %>
                <%= student.SubjectStudents.score%>
            <% } %> </td>
        </tr>
        <%  })   %>
       
    </table>
</body>
</html>